# AKERIYAN backend — all free / open-source, no paid API keys.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic-settings>=2.2
python-multipart>=0.0.9

# Speech-to-text (free, local)
faster-whisper>=1.0.3

# HTTP for the free skills (Ollama, Open-Meteo, Google News, DuckDuckGo)
httpx>=0.27
pypdf
